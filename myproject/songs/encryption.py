"""
暗号化ユーティリティ

注意: このファイルは将来の暗号化機能用に予約されています。
現在、is_encryptedフィールドは使用されていません。
"""

# 将来の実装用プレースホルダー
# from cryptography.fernet import Fernet
# 
# def encrypt_lyrics(content, key):
#     """歌詞を暗号化"""
#     f = Fernet(key)
#     return f.encrypt(content.encode()).decode()
# 
# def decrypt_lyrics(encrypted_content, key):
#     """歌詞を復号化"""
#     f = Fernet(key)
#     return f.decrypt(encrypted_content.encode()).decode()
